---
apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Provider
metadata:
  name: bitbucket
  namespace: flux-system
spec:
  type: bitbucketserver
  address: https://localhost.com/rest/api
  username: me
  secretRef:
    name: bitbucketx
